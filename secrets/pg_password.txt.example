# PostgreSQL kakeibo ユーザのパスワード雛形。
# 本ファイルは雛形のため、実運用前に以下を実施する:
#   cp secrets/pg_password.txt.example secrets/pg_password.txt
#   chmod 600 secrets/pg_password.txt
#   <安全な乱数を書き込む>
# 実体（pg_password.txt）は .gitignore で追跡対象外となる。
change-me-with-strong-random-string
